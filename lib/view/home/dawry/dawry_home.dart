import 'package:flutter/material.dart';

class DawryHome extends StatelessWidget {
  const DawryHome({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Dawry Home"),
    );
  }
}